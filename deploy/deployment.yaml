apiVersion: apps/v1
kind: Deployment
metadata:
  name: rdf-visual-transform
  namespace: argocd
  labels:
    app: rdf-visual-transform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rdf-visual-transform
  template:
    metadata:
      labels:
        app: rdf-visual-transform
    spec:
      containers:
        - name: rdf-visual-transform
          image: ghcr.io/mrkmrk00/rdf-visual-transform:a5d64e4ff39e44b444f828fc4b346e9fdb52fd42
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 100m
              memory: 256Mi
