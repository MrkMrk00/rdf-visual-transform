apiVersion: v1
kind: Service
metadata:
  name: rdf-visual-transform
  namespace: argocd
spec:
  selector:
    app: rdf-visual-transform
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
