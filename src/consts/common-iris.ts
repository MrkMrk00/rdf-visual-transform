import { FOAF, RDF, RDFS } from './rdf-prefixes';

export const RDFS_LABEL = `${RDFS}label`;
export const RDF_TYPE = `${RDF}type`;

export const FOAF_PERSON = `${FOAF}Person`;
export const FOAF_NAME = `${FOAF}name`;
